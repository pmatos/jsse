// Fixture for ./source-phase-reexport.js — one of two modules re-exporting
// the same `<module source>` binding as `mod`, star-combined in
// ./source-phase-reexport-star_FIXTURE.mjs.
import source mod from '<module source>';
export { mod };
